# http://editorconfig.org
root = true

[*] # 对所有文件生效
charset = utf-8 # 设置默认编码为utf-8
indent_style = space # 空格缩进
indent_size = 2 # tab为两个空格
trim_trailing_whitespace = true # 文件以一个空白行结尾
insert_final_newline = true # 除去换行行首的任意空白字符
end_of_line = lf
